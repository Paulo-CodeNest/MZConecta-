import {NextResponse} from 'next/server'; import {prisma} from '@/lib/prisma'; export async function GET(req:Request){const u=new URL(req.url);const niche=u.searchParams.get('niche');const min=Number(u.searchParams.get('minFollowers')||0);const max=Number(u.searchParams.get('maxPrice')||999999999);const creators=await prisma.creator.findMany({where:{niche:niche||undefined,followers:{gte:min},basePrice:{lte:max}},include:{socials:true,reviews:true},orderBy:{engagement:'desc'},take:50});return NextResponse.json(creators)}