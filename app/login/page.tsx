'use client'; import {useState} from 'react'; import {useRouter} from 'next/navigation'; import Link from 'next/link';
export default function Login(){const [email,setEmail]=useState('empresa@demo.mz'),[password,setPassword]=useState('Demo@12345'),[err,setErr]=useState('');const r=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setErr('');const x=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});if(x.ok)r.push('/dashboard');else setErr((await x.json()).error||'Erro ao entrar')}return <main className="min-h-screen grid place-items-center p-6"><form onSubmit={submit} className="card p-8 w-full max-w-md"><div className="text-2xl font-black text-teal-700">MZ Conecta</div><h1 className="text-3xl font-black mt-5">Entrar</h1>{err&&<div className="bg-red-50 text-red-700 p-3 rounded-lg mt-4">{err}</div>}<label className="block mt-5 text-sm font-bold">Email<input className="w-full border rounded-xl p-3 mt-1" value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="block mt-4 text-sm font-bold">Password<input type="password" className="w-full border rounded-xl p-3 mt-1" value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="w-full mt-6 bg-teal-700 text-white p-3 rounded-xl font-bold">Entrar</button><p className="text-sm text-slate-500 mt-5">Ainda não tens conta? <Link className="text-teal-700 font-bold" href="/register">Criar conta</Link></p></form></main>}