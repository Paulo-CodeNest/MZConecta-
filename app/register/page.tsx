'use client'; import {useState} from 'react'; import {useRouter} from 'next/navigation';
export default function Register(){const [form,setForm]=useState({name:'',email:'',phone:'',password:'',role:'COMPANY'});const [err,setErr]=useState('');const r=useRouter();async function submit(e:React.FormEvent){e.preventDefault();const x=await fetch('/api/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});if(x.ok)r.push('/onboarding');else setErr((await x.json()).error||'Não foi possível criar a conta')}return <main className="min-h-screen grid place-items-center p-6"><form onSubmit={submit} className="card p-8 w-full max-w-md"><div className="font-black text-2xl text-teal-700">MZ Conecta</div><h1 className="text-3xl font-black mt-4">Criar conta</h1>{err&&<p className="text-red-600 mt-3">{err}</p>}<div className="grid grid-cols-2 gap-2 mt-5">{[['COMPANY','Empresa'],['CREATOR','Criador']].map(([v,l])=><button type="button" key={v} onClick={()=>setForm({...form,role:v})} className={`p-3 rounded-xl border ${form.role===v?'border-teal-700 bg-teal-50':''}`}>{l}</button>)}</div>{[['name','Nome'],['email','Email'],['phone','Telefone'],['password','Password']].map(([k,l])=><label key={k} className="block mt-4 text-sm font-bold">{l}<input type={k==='password'?'password':'text'} required={k!=='phone'} className="w-full border rounded-xl p-3 mt-1" value={(form as any)[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/></label>)}<button className="w-full mt-6 bg-teal-700 text-white p-3 rounded-xl font-bold">Criar conta</button></form></main>}